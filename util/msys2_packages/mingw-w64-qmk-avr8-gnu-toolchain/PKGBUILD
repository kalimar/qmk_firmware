_realname=qmk-avr8-gnu-toolchain
pkgbase=mingw-w64-${_realname}
pkgname="${MINGW_PACKAGE_PREFIX}-${_realname}"
pkgver=3.5.4
pkgrel=1
arch=('any')

build() {
  echo "build"
}

package() {
  echo "package"
}